# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/
.vscode/
# pubspec.lock is deliberately committed. This is an application, not a
# library, and the Fly image builds from it — ignoring it would let a deploy
# resolve a different dependency set than the one tested here.

# Platform
ios/Pods/
ios/.symlinks/
macos/Pods/
android/.gradle/
android/local.properties
android/key.properties
**/Flutter/ephemeral/
**/Flutter/Generated.xcconfig
**/Flutter/flutter_export_environment.sh

# Python tooling venv (tool/)
.venv/
__pycache__/

.DS_Store

# Build output
build/

# Golden diff artifacts
test/failures/
